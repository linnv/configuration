# RabbitMQ Configuration
loopback_users.guest = false
listeners.tcp.default = 5672
management.tcp.port = 15672

# Enable guest user for external connections (for demo purposes only)
default_user = admin
default_pass = admin

