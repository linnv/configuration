package utility

import "testing"

func TestUniqueSlice(t *testing.T) {
	// a := []int{1, 2, 2, 34, 5, 66, 4, 5, 4}
	// b := UniqueSlice(a)
	// //@todoDelelte
	// fmt.Printf("a,cap(a):%d,len(a):%d  : %+v\n", a, cap(a), len(a))
	// //@todoDelelte
	// fmt.Printf("b,cap(b):%d,len(b):%d  : %+v\n", b, cap(b), len(b))
	// t.Log("test uniquing slice  :good")
}
