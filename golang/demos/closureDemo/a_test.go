package newDir

import "testing"

func TestJustDemo(t *testing.T) {
	// JustDemo()
	Trap()
	// var a []functionTemplate
	// a = append(a, fs)
	// a = append(a, fs)
	// for k, v := range a {
	// 	fmt.Printf("k: works %v\n", k)
	// 	v(k)
	// }
	// r := ClosureDemo(999, func(line string) string {
	// 	//here we implement the closure function in closureDemo function
	// 	return line + "implement closure function"
	// })
	// fmt.Printf("r: %+v\n", r)
	// + "closure function"
}
