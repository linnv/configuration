syntax = "proto3";
package  protocalDemo;

enum FOO { X = 0; };

message Test {
	string label = 1;
	int32 type = 2 ;
	int64 reps = 3;
	/* group OptionalGroup = 4 { */
	/* 	string RequiredField = 5; */
	/* } */
}
